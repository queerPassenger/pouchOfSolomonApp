import React, { ReactElement } from 'react';
import { View, Text } from 'react-native';

const AddTransaction: React.FC = (): ReactElement => {
    return(
        <View>
            <Text>
                Add Transaction
            </Text>
        </View>
    )
}
export default AddTransaction;